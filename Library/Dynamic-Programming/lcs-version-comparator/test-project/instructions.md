# User Guide: Config Version Comparator (LCS)

This project demonstrates the use of Dynamic Programming (LCS) to perform sequence alignment and difference detection between two configuration states.

## Operation
1. Navigate to the `test-project` directory.
2. Run the application:
   ```bash
   python app.py