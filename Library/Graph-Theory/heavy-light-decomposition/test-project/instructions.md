# Test Project Instructions

1. Open a terminal in `Library/Graph-Theory/heavy-light-decomposition/test-project`.
2. Run the demo:

```bash
python app.py
```

The script prints a few sample path and subtree operations and compares them against a naive check.
