# User Guide: Malicious URL Security Guard

This project demonstrates the **Bloom Filter**, a probabilistic data structure used for high-speed membership testing.

## Operation
1. Navigate to the `test-project` directory.
2. Ensure you have the `url_database.csv` in the same folder.
3. Run the application:
   ```bash
   python app.py