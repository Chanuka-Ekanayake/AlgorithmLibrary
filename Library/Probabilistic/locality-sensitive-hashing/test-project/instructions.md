# User Guide: Similar Model Finder (LSH)

This project demonstrates **Approximate Near Neighbor (ANN)** search using Random Projections.

## How to Test
1. **Navigate** to the `test-project` folder.
2. **Ensure NumPy is installed**:
   ```bash
   pip install numpy